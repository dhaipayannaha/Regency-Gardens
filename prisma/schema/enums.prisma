// next
enum Role {
  USER
  AGENT
  ADMIN
}

enum ListingType {
  SALE
  RENT
}

enum PropertyStatus {
  ACTIVE
  PENDING
  SOLD
  RENTED
  DRAFT
}

enum AuthProvider {
  googleId
  CREDENTIAL
}

enum InquiryStatus {
  PENDING
  RESPONDED
  CLOSED
}
